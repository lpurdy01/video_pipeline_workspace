# Immutable review records

No reviews have been completed or imported yet. Use `compile.py --import-review` with an actual review record following `../ARCHITECTURE.md`. One unique ID per record; corrections create a new record and preserve the original. Git history and controlled integration are needed in addition to the CLI's no-overwrite rule.
