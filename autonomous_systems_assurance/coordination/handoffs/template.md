# Handoff: <job-id> / <version>

- Author/session and role:
- Date/time with timezone:
- Job card:
- Input baseline and hashes:
- Output file paths and SHA-256 values:
- Completion state and any blocked deliverables:
- Writes paused for integration: yes/no

## Findings and proposed changes

List source and claim IDs, what each changes, source-region locators and scope. Separate direct source statements, inference and project proposals. Include contrary evidence and the strongest unresolved challenge.

## Search and inspection record

Record queries, retrieval dates, original publishers, inspected sections, failed retrievals, editions, jurisdiction and status. Identify reused originating evidence so repeated reporting is not counted as independent support. Point to local permitted snapshots and hash manifests; do not paste licensed full standards or credentials.

## Review provenance

If a review was performed: reviewer/session, model/provider if applicable, author relationship, question asked, exact input/package digest, inspected source regions, result, limitations and human disposition. If no review occurred, say so. This narrative is not a substitute for compiler-schema review records.

## Verification

Commands/checks run, compiler version, exact input baseline, report location and outcome. State remaining parse errors, stale packages, unsupported uses and coverage limitations. A successful parse is not content acceptance.

## Impact and next action

Affected paper sections, video beats, visual assumptions, wiki pages and release material. Include any ID remapping proposal, shared-file edits for the integrator, and questions that require a human decision. Do not edit another lane to implement these proposals.
