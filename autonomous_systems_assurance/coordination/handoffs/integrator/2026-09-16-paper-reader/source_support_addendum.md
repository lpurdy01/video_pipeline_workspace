# Digest-pinned source-support addendum

**Review date:** 2026-09-16  
**Scope:** current post-whitepaper `source_support` packages for the fourteen worked-case claims.

Fourteen candidate model review records are staged in `source_support_candidates/`; none has been imported into `verification/reviews/`, and no production registry, manifest, or manuscript was changed by this pass. Each candidate was repinned after the final narrow factual-scope revision to the package digest from the current compile.

**Result:** 11 pass candidates and 3 uncertain candidates.

The three known context limitations remain uncertain:

* `C-002`: the FAA TSO statement has only one short excerpt, with no snapshot or full local context.
* `C-AUTH-002`: the FAA roadmap update wording has only a short excerpt and no captured context for the learned-versus-learning section.
* `C-AUTH-008`: the NHTSA ADS page has three short excerpts with no full local context.

The other eleven candidates pass only their bounded source-support question. Their limitations preserve document status, jurisdiction, method scope and the distinction between a source-backed finding and an evidence-backed physical-system safety case. An integrator should recompile immediately before any import; changes to a manifested artifact or case node invalidate these exact digests.
