# Project log

## [2026-09-08] ingest | Foundation and primary-source seed

Reviewed the earlier wiki, source pipeline, prototype, whitepaper construction, saved reviews, video production and resource release artifacts. Created a separate workspace, source register, provisional claims, research agenda, assurance proposal and production plan. Original source context and independent review remain explicit work items.

## [2026-09-08] decision | Audience, narrative and infrastructure

Levi selected technical generalists/engineers and a practical assurance-architecture question. Start with a car perceiving/navigating/avoiding and let it take off near the end to examine changed assumptions. Build toward a fuller verification compiler. Gemini text/scratch-TTS/rendered-visual processing approved; human audio/timing stays local. DAA experience is about five years old and supplies motivation rather than present-day evidence.

## [2026-09-08] ingest | Parallel contribution and compiler review

A separate research agent added Texas program and first-responder listing sources under `research/contributions/road_air/`, preserving uninspected individual authorization and Zipline grant gaps. Integrator added explicit claim tags and the findings artifact to the compiler manifest after handoff. A coordination agent created the multi-agent protocol and reviewed the compiler for stale-review, dependency and concurrent-read defects. Main-agent fixes and seeded tests address those findings; no research claim has been promoted to independently verified.

## [2026-09-08] ingest | Round-2 ROAD, AIR and METHOD handoffs

Three concurrent lanes returned 14 ROAD sources/claims, 13 AIR sources/14 claims and 17 METHOD sources/19 claims. Their findings were saved under exclusive contribution directories and added to the manifested integration graph without flattening lane provenance. Three repeated URLs were marked related rather than independent. The compiler now reports 61 sources, 66 provisional claims, 264 planned reviews, zero review records and 368 blockers.

## [2026-09-08] ingest | Supporting-document retrieval

The integrator retrieved full local copies of the FAA AI roadmap, EASA Issue 2 and Proposed Issue 3 papers, UK CAA CAP3127, NASA runtime-assurance paper, FAA/GA-ASI validation report, TxDMV program page and UK Protector announcement. SHA-256 hashes and exact retrieval scope are recorded in `research/retrieval_status.md`; files remain under ignored compiler output pending context extraction. Zipline docket letters and NHTSA AQ26002 associated records returned HTTP 403; the TxMCCS lookup returned no readable content; actual GA-ASI authorization status and the Protector certificate remain unacquired. Human browser/docket attention is explicitly required for those items.

## [2026-09-09] ingest | Book dump verification and Deep Learning HTML intake

Verified four local book artifacts and added source records `S-016` through `S-020`. The Springer *Autonomous Driving* PDF is byte-identical to the publisher’s canonical open-access PDF. *Engineering a Safer World* matches MIT Press title/author/open-access metadata. The two *Verifiable Autonomous Systems* files are byte-identical duplicates. The local *Probabilistic Robotics* file is explicitly an early draft marked not for distribution and remains provisional rather than a verified published edition.

The public `deeplearningbook.org` landing page was scraped only through its own linked HTML pages. Raw HTML, page-aware cleaned text, hashes and a reproducible extractor are kept under ignored `out/`. The book source record distinguishes public online access from PDF distribution. The remaining critical acquisition gaps are Kritzinger’s *Aircraft System Safety*, Gratton’s *Initial Airworthiness*, and Fossen/Pettersen/Nijmeijer’s *Sensing and Control for Autonomous Vehicles*.

## [2026-09-15] ingest | Authority-document intake and browser retrieval

Normalized the user-supplied FAA Zipline 19111B/19111C decisions, TSO-C211a, Amazon 19031B and NHTSA AQ26002 opening resume under ignored source output, with immutable snapshots, hashes and UTF-8 context. Added the five scoped `INTAKE` claims and source records. The Zipline documents explicitly preserve the distinction between an operating exemption and DAA-system authorization; TSO-C211a separately states that installation requires separate approval. Amazon 19031B is retained as a contrary boundary case because its requested removal of visual observers with onboard DAA was still under review.

A Chromium pass reached the public Regulations.gov document pages and the JavaScript TxMCCS operator lookup. It captured the current Tesla Robotaxi record as a dated road-authorization fact, including Cybercab rows, while preserving that it is not technical-safety evidence. The project decision now treats Tesla as an optional opener, not the project’s thesis. NHTSA's dynamic AQ record still denied browser access beyond the now-acquired opening resume; GA-ASI authorization and the Protector certificate remain unresolved.

## [2026-09-15] decision | Frozen learned-systems assurance direction

The first adequacy claim now concerns a frozen, versioned trained model rather than a system that learns during operation. The project will test a proposed assurance case built from separate contracts for operating scope, training lineage, scenario/statistical evidence, uncertainty, closed-loop behavior, intervention/recovery, change control and claim-specific analysis. It explicitly rejects a composite model-safety score.

The next research wave has three independently writable lanes: an authority/standards map, an evidence-method map and an adversarial gap challenge. A final statement that no adequate public end-to-end process was found requires the resulting dated, scoped search record; the project will not infer universal absence or speculate about proprietary Tesla evidence.

## [2026-09-16] integrate | Existing learned-system assurance baselines and method evidence

Three concurrent research lanes and an independent Gemini grounded-discovery pass completed. The challenge lane established that the project must not claim that learned-system assurance processes do not exist: AMLAS is a public learned-component methodology; UK MAA guidance supplies a case-specific military path; and EASA NPA 2025-07(B) is a detailed but non-final civil proposal with explicit high-consequence limits. The project now compares and operationalises these methods instead of presenting its evidence contracts as novel.

Authority duplicate documents were retained only as upgraded inspection provenance and mapped to their existing evidence families. The later NASA runtime-assurance presentation is explicitly non-independent of the earlier NASA method family. Gemini output is retained only as a lead list; AMLAS was independently captured, while the remaining leads require source retrieval and rights review. The frozen compiler build is structurally valid with 89 sources, 94 claims and zero review records; all new claims remain provisional.

## [2026-09-16] implement | Executable worked assurance-case graph

Added a machine-readable hypothetical road-perception release with one occluded-conflict hazard, five explicit assumptions, eight evidence obligations and five planned evidence artifacts. The compiler now validates and graphs system releases, assumptions, hazards, evidence artifacts and obligations. Linked claim review packages include hashes of their case nodes, so a changed obligation or assumption invalidates affected reviews and propagates to downstream artifacts.

The initial case intentionally remains blocked: its release is only defined, all assumptions and obligations are open, and all evidence artifacts are planned. This makes the missing world/system evidence explicit without treating a source list, test metric, LLM judgment or model confidence as a safety score.

The AMLAS, MAA and EASA comparison is also encoded as typed external-method and method-to-obligation mapping nodes. Their status, scope and coverage labels now participate in change impact and linked review-package digests; the graph preserves that a methodology or proposal does not close the underlying hazard evidence.

## [2026-09-16] verify | First worked-case model review pass

Expanded the case to matched road and airborne hypothetical encounters: two frozen releases, two hazards, ten assumptions, sixteen obligations and ten planned evidence artifacts. The compiler now reports verification-gate completion separately from safety: 39 of 56 worked-case review packages currently pass (69.6%), including 39 of 42 model-eligible packages (92.9%). No human disposition exists, so human-disposition coverage remains 0%.

Three source-support reviews remain uncertain because their referenced FAA/NHTSA material has only short excerpts rather than captured full context. The machine-generated first-pass report and 14-item human review queue remain under ignored compiler output. Gemini cloud review was attempted but the configured project reported depleted prepaid credits before a usable response; the completed first pass therefore records three separately scoped local-model reviews, not a Gemini result or a human decision.

## [2026-09-16] compose | Reader-facing whitepaper and independent review refresh

Replaced the composition seed with a 5,460-word reader-review whitepaper and a linked HTML/PDF prototype. The draft begins with the occluded-road encounter, states the proposed eight-contract assurance rule, bridges conventional traceability to a frozen trained release, compares AMLAS/MAA/EASA scope, carries the same argument into airborne DAA, and exposes the still-open release/evidence obligations. It does not claim that a real vehicle, aircraft, DAA product, or proprietary model is safe.

Three independent model-review lanes examined scope, reader clarity and compiler integration, then produced digest-pinned source-support, challenge and cross-artifact records for all 14 worked-case claims. A fourth bounded pass covered the three paper-only cited claims. The current compiler result must be read as review-gate status only: human disposition remains zero, and the worked case remains blocked by its two defined-only releases, ten open assumptions, ten planned evidence artifacts and sixteen open obligations. The exact current counts are maintained in the generated compiler report rather than hardcoded into the prose.

The HTML/PDF source cards link to original sources and state limits; the compiler report remains the live status authority. A minimal Gemini health check failed again with `429 RESOURCE_EXHAUSTED` and an explicit depleted-prepayment message, so no Gemini review was added. The sanitized diagnosis and safe retry sequence are recorded in `planning/gemini_integration_status.md`.

## [2026-09-19] implement | Portable Xteink review surface

Pinned CrossPoint Reader as a submodule and inspected its X4 Pro build profile, file dispatch, EPUB/CSS limits, transfer API, and host-test boundary. The firmware does not provide a PDF reader or a full device emulator; it does provide EPUB reading, bookmarks, screenshots, USB Drive on X4 Pro, and a documented HTTP upload endpoint.

Added a local PDF-to-CrossPoint EPUB toolkit under `resource_composition/crosspoint_review/`. Each generated chapter maps to one original PDF page and carries its stable locator; the adjacent manifest records original-PDF, EPUB, and per-page extracted-text SHA-256 digests. A generated 22-page whitepaper review copy structurally validated locally. It has not been opened, bookmarked, or otherwise tested on physical hardware, and reader actions remain manual review inputs rather than compiler review dispositions.

## [2026-09-19] implement | Simulator-backed conversion and review loop

Extended the review surface into a staged PDF conversion pipeline: PyMuPDF block extraction, conservative two-column repair, Tesseract fallback for text-empty pages, digest-pinned EPUB output, source-page render capture, and a deterministic extraction-risk queue. A model reviewer is opt-in per input because arbitrary PDFs can be rights-sensitive; it is advisory and cannot rewrite source text or become a human disposition.

Pinned the official CrossPoint SDL simulator, initialized the reader's FreeInk dependency, and added an X4 Pro adapter that prepares a generated EPUB as simulated SD-card state. The current whitepaper run produced 22 source-page images and five explicit column-order checks.

The native X4 Pro firmware build subsequently completed in a local, ignored toolchain and rendered the generated EPUB through SDL. Two scripted 480×800 firmware BMP captures are digest-recorded in the review packet and passed the nonblank/geometry capture checks. This verifies EPUB parsing, pagination, reader rendering, and scripted input in the firmware path; it does not verify physical panel waveform, ghosting, battery behavior, or semantic/source-layout fidelity. The five two-column pages remain explicitly queued for source-image comparison.

## [2026-09-22] compose | FAA lead and evidence-bridge diagrams

Captured the FAA *Roadmap for Artificial Intelligence Safety Assurance*, Version I, PDF p. 14 as scoped source region `S-AUTH-001-R4` and added `C-AUTH-010`. The paper now begins with the roadmap's learned-versus-designed implementation problem in plain language: conventional requirements and assurance remain relevant, but they do not provide a direct requirement-to-weight explanation for a learned release. Two rendered diagrams then make the proposed response concrete: an evidence bridge from hazard and operating domain through release lineage, scenario evidence, recovery, and change control; and six skimmable questions that bound a frozen release's authority.

This reader-facing revision changed manifested paper and source artifacts. Existing model-review records are intentionally preserved as historical but stale; they do not count toward the current compiler score until a new review pass evaluates this version.

## [2026-09-22] integrate | Long-form standards and risk-evidence expansion

Added independent `risk_evidence` and `standards_authority` contribution lanes. Their primary-source findings correct the project framing: FAA work, AMLAS, EASA DS.AI, road standards and guidance are evidence of active methods and pathways, but their status and scope differ. The project will ask about an unfinished, fragmented public route for the high-consequence learned-component case rather than claim no method exists. US manufacturer self-certification is recorded as certification to applicable FMVSS, not a blanket learned-model safety certification. No source supports attributing unfinished standardisation to inadequate mathematical rigor or too few qualified people.

The whitepaper/video now have a 14-part, long-form structure and a 34-minute isolated treatment. Two new original diagrams make the system and evidence architecture concrete: nested assurance shells around the learned release, and a conditional risk graph that separates hazard exposure, measured performance, uncertainty, recovery and bounded authority. The compiler now validates optional `risk_context` nodes so a metric's assumptions and limits participate in review invalidation; it does not calculate a composite safety score. Browser and direct-download attempts did not yet yield full local snapshots for the NHTSA interpretation or SAE J3321 public scope page; those records remain explicitly context-limited.

## [2026-09-22] integrate | Authority-and-risk coverage map and denominator limits

Added a video-ready coverage map of AMLAS, MAA, FAA, EASA and NHTSA roles. It is deliberately a scope-and-status map, not an approval ladder. The retained EASA proposal distinguishes Levels 1A–3A from `3B Reserved`; it therefore cannot support calling 3B an approved fully autonomous category. Its Table 2 supplies proposed per-operational-hour likelihood bands and accepts no H1 fatality potential across those bands, while retaining qualitative engineering judgment as a required part of risk assessment.

Added retained NHTSA and NCEES records. NHTSA incident reports are useful public signals but the agency's own data dictionary says they can be incomplete, unverified, non-normalized and duplicated, so the paper does not convert them into an autonomous-vehicle safety rate. The historic NHTSA quiet-car rule documents a missing powertrain-VMT denominator in its EV/hybrid comparison; the paper now uses that as a reason to show an exposure ledger rather than a headline EV-versus-ICE rate. NCEES supports a narrow current exam-taxonomy observation only. The proposed causal story that few qualified people explains unfinished standards remains explicitly unsupported and is withheld.

## [2026-09-22] verify | Current model-review refresh and quota resilience

Imported 91 current, digest-pinned Gemini first-pass review records across source-support, challenge and cross-artifact tasks during the refresh. The compiler records 68 current passing packages and 24.6% automated review coverage; its 18.4% worked-case verification-gate coverage remains far below release readiness because no human disposition or real worked-case evidence exists. A parallel batch then reached Gemini 3.1 Pro's daily 250-request project quota. The review runner now keeps completed candidates importable and records provider errors separately, so a quota limit cannot discard earlier completed reviews or be misclassified as a claim verdict.

## [2026-09-23] verify | Pre-human review target and source-context correction

The compiler now reports 100% automated coverage for the 19 worked-case claims: all 57 source-support, challenge and cross-artifact packages have a current model pass. The overall worked-case figure remains 75.0% because the 19 required human-disposition packages remain deliberately unfilled. This meets the project’s pre-human automation target; it does not establish publication readiness, factual certainty, safety, or closure of the hypothetical road/air cases.

Introduced a source-context recovery staging process and retained three official source captures. It corrected the FAA roadmap locator offset between physical and printed PDF pages and promoted eight source regions to full local context, reducing source-context blockers from 123 to 115. Direct ISO and NHTSA retrieval returned HTTP 403 and remains explicitly incomplete. A Flash challenge objected that EASA's proposed lifecycle cannot cover the project’s highest-consequence case; a Pro review found the bounded comparison claim accurate. Both records remain visible for later human disposition rather than being averaged away.

## [2026-09-25] audit | Newer Gemini editorial lead and airborne-recovery refinement

A bounded Gemini 3.8 Flash argument audit was run as advisory lead generation only. After correcting its prompt to prevent clipped context from being mistaken for missing manuscript text, the locally triaged result preserved the paper’s existing EASA scope qualification, confirmed the distinction between GA-ASI integration material and learned-model evidence, and identified a constructive gap in the worked airborne case.

The air case now requires evidence-backed traffic-and-clearance observation for an active escape maneuver; a later landing or diversion plan cannot substitute for immediate collision avoidance. That requirement is reflected in `AS-AIR-004`, `EA-AIR-004`, `O-AIR-006`, the method crosswalk, paper and script. Because the graph change materially alters digest-pinned review inputs, prior model reviews are stale pending a paced refresh. Gemini requests are now serialized with a documented inter-request interval; no model output is treated as source evidence or human disposition.

## [2026-09-25] compose | Shared vision cue and AMLAS training failure modes

The paper and narration now use the same narrow hypothetical vision task: a frozen road or air model supplies a timed candidate image location/bearing and uncertainty/unknown signal; separate tracking, geometry, action authority and recovery evidence determine whether any maneuver is justified. This is a shared interface, not a transferable model or approval. The reader diagram `vision_training_contract.svg` shows distinct road and air releases and the physical limit of an unobservable hazard.

A follow-up inspection of the retained AMLAS v1.1 PDF recorded full-context regions for data relevance/completeness/accuracy/balance, overfitting and internal-test leakage, and independent challenge/verification. The official *Deep Learning* online Chapter 5 provides the narrow underfit/overfit capacity illustration. Four new provisional claims (`C-TRAIN-001`–`004`) are linked to the paper, script, source cards and the worked-case evidence nodes. The project-proposed grouped holdouts, unknown handling and scene partitions are labeled as design choices, not AMLAS mandates. These changes intentionally invalidate earlier digest-pinned review packages until a paced refresh.

## [2026-09-25] verify | Shared vision training-case reader pass

Regenerated the 39-page whitepaper PDF and linked HTML after importing 69 digest-current, serially paced Gemini 3.8 Flash first-pass records (67 pass, two uncertain, no provider errors). All 12 source-support, challenge and cross-artifact reviews for `C-TRAIN-001` through `C-TRAIN-004` pass. The compiler now reports 97.1% automated worked-case review coverage, 72.8% overall worked-case review-gate completion and 0% human disposition. These are review-process measures, not a model-safety result; hypothetical release evidence and historical objections remain open.

The two new uncertain source-support results exposed a physical/printed page offset in the NIST RMF record and a short-excerpt-only NHTSA ADS 2.0 record. A native 36-page copy of the original NHTSA guidance was retained from a public mirror after the official host returned HTTP 403; the native PDF title, page count and cited passages were checked against NHTSA's web-accessible original. Integrating those repaired source records into the compiler would invalidate all 69 current review digests through shared manuscript dependencies. Their source-record update is therefore queued for the next coordinated review refresh; the retained NHTSA PDF and provenance are indexed in `research/local_source_library.md`.
