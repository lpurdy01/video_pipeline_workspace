# Human Audio

Place local human recordings for this video here.

Recommended filenames:

```text
readthrough_YYYY_MM_DD.wav
comments_YYYY_MM_DD.wav
01_generation_got_cheap.wav
02_chat_log_fallacy.wav
...
```

Raw human voice stays local by default. Use local transcription first, then send
only text transcripts, summaries, or rewrite notes to approved compute APIs.

